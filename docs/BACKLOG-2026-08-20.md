# PriceSniffs backlog — work breakdown, 2026-08-20

This is a planning document only. No application code was changed to produce
it. It breaks the 21 owner-approved backlog items into sub-tasks small enough
for one agent in one sitting, states what is actually known about each
(re-verified against the repo where that was cheap, marked `unverified`
where it was not), and gives dependencies, file-collision risk, and a
recommended dispatch order.

**Method.** A fresh shallow clone of `claude/scentday-retailer-registry-h92tth`
at commit `16297ca`, `npm ci`, both typechecks, and a full test run (confirmed
clean, 1056/1056 across 49 files — matches the stated baseline exactly).
Every claim below that came from grepping or reading a file names the file;
every claim from GitHub Actions run history names the run; anything from the
original brief that I did not re-check is marked **reported, not
re-verified**.

---

## Read this before dispatching anything

### A. Automation is already working four of these items, right now

`list_triggers` shows an **hourly** Routine (`trig_01N9Gq6DnNrY7ke7VqGbtjJq`,
"advance the long-running backlog," last fired 2026-08-20T21:12Z, fires again
at :10 past every hour) that autonomously picks one of five items each tick:
brand website coverage, fragrance-notes coverage, notes attribution
(`pickNotes` provenance), per-fragrance official+Fragrantica links, and
catalogue-push durability. Those are **exactly items 12, 9, 10 and 11 of this
backlog**, plus a fifth item not on it. It touches `demo/brandSites.ts`,
`scripts/build-demo-catalogue.ts` (`pickNotes`), and fragrance-detail
rendering in `demo/app.ts` — the same files a dedicated agent on 9/10/11/12
would touch.

**Recommendation:** before dispatching a bespoke agent on 9, 10, 11 or 12,
run `git log --oneline -20` on the branch and check whether the last hour's
commits already cover it. If you want faster progress than one item per hour,
dispatch a larger, explicitly-scoped agent instead of letting two agents
converge on `pickNotes` or `brandSites.ts` in the same window — pick a
different item from the five than whatever the routine most recently touched,
or disable/pause the routine (`update_trigger` with `enabled: false`) for the
duration of a focused push and re-enable it after.

### B. Two changelog Routines exist, not one, and both fire tonight

The brief names `trig_01SsyvoqkDoQo6E3Djjdtsmq` ("daily update log, weekdays
22:00 UTC"). There is a **second, older** one still enabled:
`trig_01PFKVJvLMeWz7UvXdiBCLhY` ("Daily changelog entry," daily at 23:55 UTC,
runs `scripts/changelog-suggest.ts` and acts on its exit code). Both write to
`demo/changelog.ts`. At the time of this document (2026-08-20 22:02 UTC) the
newer one is due to fire in minutes (`next_run_at` 22:08Z) and the older one
at 23:55Z — **both today**. `demo/changelog.ts`'s newest entry is still 18
Aug, so neither has run today yet.

**Recommendation:** do not dispatch item 20 manually before ~00:00 UTC.
Check `demo/changelog.ts` after both have had a chance to fire; only act if
both produced nothing for today's four reader-visible changes, or produced
duplicate/conflicting entries (in which case reconcile by hand rather than
letting a third agent add a third entry).

### C. The stated concurrency hazard does not match the workflow files as they stand today

The brief states `price-verify.yml` and "the probe workflows" share a
concurrency group and silently cancel each other. I read the actual YAML:

| Workflow | `concurrency.group` | `cancel-in-progress` |
|---|---|---|
| `catalogue-daily.yml` | `catalogue` | `false` |
| `price-verify.yml` | `price-verify` | `false` |
| `harvest-one-shop.yml` (the probe workflow) | **none set** | n/a |

Three different groups, not one shared one, and both real groups queue
(`cancel-in-progress: false`) rather than cancel. I also pulled the last 35
runs of `harvest-one-shop.yml` via the GitHub API (several dispatched
seconds apart, 06:57–16:11 UTC today) — every one completed `success` or
`failure`; **none show `cancelled`**. `harvest-one-shop.yml`'s own header
comment explains why: it was created *today* (07:54 UTC, commit history
confirms) specifically to stop diagnostic dispatches from queuing behind the
90-minute `catalogue` group that `catalogue-daily.yml` holds — which was
almost certainly the real mechanism behind the three lost batches the brief
describes, from before this workflow existed.

**This does not mean rapid-fire dispatch is free.** I could not check the
account-level concurrent-job ceiling GitHub enforces regardless of
per-workflow groups, and every actor run still spends real Apify credit
whether or not it queues. So the plan below keeps the owner's instruction —
**dispatch probes one shop at a time, confirm from the job log which shop
ran, wait for completion before the next** — as a live rule for cost control
and log attribution, not because the specific collision mechanism described
still exists. Worth telling the owner the mechanism was found and fixed
today, so the workaround doesn't need to be treated as permanent.

### D. `demo/app.ts` collision map

5,009 lines, one module, called out in the brief as today's collision point.
Items that touch it: **6** (`trustpilotWidget`, ~line 2090), **11** (fragrance
detail block), **15** (`retailersPanel`/Most Stocked rendering, ~line 2056 and
the `POPULAR` slice at ~line 318 — ranking itself lives in `demo/data.ts` and
`scripts/build-demo-catalogue.ts`, see item 15), **16** (`brandView`, ~line
2163), **8** (wherever a rating renders, likely near the same fragrance-detail
block as 11). Two testable modules were already carved out of it today for
exactly this reason — `demo/volumeBands.ts`, `demo/listSort.ts` — confirmed
present. **Any sub-task below whose logic can be unit-tested should follow
that pattern: extract a pure function into its own `demo/*.ts` module with
its own test file, and give `app.ts` only the minimal call/render site.**
That shrinks the diff any one agent needs inside the 5,009-line file and
shrinks the odds of two agents editing the same lines.

### E. Apify cost discipline (items 1, 2, 3, 18, 19)

Apify is on the free plan with a small monthly credit (**reported by the
owner, not verifiable from the repo** — nothing in `apifyActor.ts` or the
docs states a dollar figure). `MAX_ACTOR_PAGES_PER_RUN = 10`
(`src/catalogue/apifyActor.ts:114`, confirmed) caps each run, but runs still
cost money. Budget actor dispatches like the probe rule above: one shop,
confirm the log, decide before the next.

---

## Items 1–3: extraction from the three proven-retrievable retailers

**Known (re-verified via `16297ca`'s commit and `apifyActor.ts`):** the
leaked race-timer bug that made successful actor runs look hung was fixed
today. Four real job logs prove retrieval works and is fast: John Lewis 19–24s
(two runs), Superdrug 27s, Zara 23s (see item 18) — all previously
miscategorised as stalls. `MAX_ACTOR_PAGES_PER_RUN = 10` confirmed.
`harvest-one-shop.yml` (confirmed to exist, created today, no concurrency
group) is the correct dispatch target — **not** `catalogue-daily.yml`,
which pulls in a 90-minute pipeline for a one-shop question.

**What is genuinely unknown:** whether `__NEXT_DATA__` (John Lewis) and
`spartacus-app-state` (Superdrug) contain the fields a parser needs
(name, price, currency, availability, image, canonical URL), and whether
Selfridges' RSC-streamed markup has *any* machine-readable product data at
all (JSON-LD and hydration-blob hypotheses both already ruled out per the
brief — **reported, not re-verified**, since re-testing that would itself
require a probe dispatch).

### Sub-tasks

**1a. Capture one raw render each, John Lewis and Superdrug.** Dispatch
`harvest-one-shop.yml --dry-run --shop=john-lewis` and, as a **separate,
later** dispatch after confirming the first shop and run from the job log,
`--shop=superdrug`. Save the full page source each returns (not just the
harvest summary) to `data/` or attach as a workflow artifact. Done when both
raw captures exist and are readable outside the job log.
*Depends on: nothing. Collision risk: none (no code touched, single workflow
dispatch each, done serially per rule C).*

**1b. Inspect the John Lewis `__NEXT_DATA__` block for parseable fields.**
Read the captured blob from 1a, locate the product record inside it, and
write down (not code — a findings note) exactly which JSON path holds name,
price, currency, stock and image. Done when the note either names a working
path or concludes the data isn't there.
*Depends on: 1a (John Lewis capture). No file collision — a findings note,
not code.*

**2b. Inspect the Superdrug `spartacus-app-state` block**, same shape as 1b,
for the SAP Commerce/Hybris state shape.
*Depends on: 1a (Superdrug capture).*

**1c/2c. Write and unit-test an extractor** for whichever of John
Lewis/Superdrug 1b/2b found usable, following the existing pattern in
`src/catalogue/jsonld.ts` / `shopifyJson.ts` (parser separate from retrieval,
unit-tested against a fixture of the captured blob, never against the live
site). One PR-sized task per retailer. Done when `npx vitest run` covers the
new parser and it correctly extracts price+currency+stock from the saved
fixture.
*Depends on: 1b or 2b concluding the data is there. Collision risk: new file
each (e.g. `src/catalogue/nextDataExtract.ts`), no shared-file risk between
the two, but both eventually register with `apifyActor.ts`'s dispatch table —
sequence 1c and 2c's registration edits rather than landing both in the same
window.*

**3a. Capture one Selfridges render** and inventory what markup actually
exists — script tags, any embedded state object regardless of name, response
headers. This is explicitly the hardest of the three per the brief (JSON-LD
and hydration-blob already ruled out), so treat this as a spike: it may
conclude "no free extraction route exists here."
*Depends on: nothing beyond a probe dispatch. Sequence after 1a/2a so the
cheaper, more likely wins get actor budget first.*

**3b. Decide and record** whether Selfridges extraction is worth further
actor spend, based on 3a. A "no viable route found" conclusion is a valid,
useful outcome — write it down in the registry entry's comment (the existing
house style, see `src/config/retailers.ts`) so a future agent doesn't repeat
the spend.

---

## Item 4: `www.` subdomain bug

**Known, fully re-verified.** `src/catalogue/sitemapCrawl.ts:234-235` builds
`https://www.${retailer.domain}...` unconditionally. `scripts/currency-probe.ts`
strips a leading `www.` instead, at lines 102 and 248
(`retailer.domain.replace(/^www\./, '')`), and does not have this bug. This is
not theoretical: today's real Morrisons harvest probe (job 96495029195, run
32390450886, logged in `src/config/retailers.ts` around line 5187) shows
`https://www.groceries.morrisons.com/sitemap.xml: HTTP 403` — the exact
failure mode this bug produces — though that run still found 182 URLs via
robots.txt's own `Sitemap:` lines, masking the bug's impact there. Affects
Asda, Morrisons, and any future subdomain-shaped retailer.

### Sub-tasks

**4a. Fix `sitemapCrawl.ts`** to build the URL the way `currency-probe.ts`
already does — strip an existing leading `www.` rather than prepending one,
so a bare domain still gets `www.` added but `groceries.morrisons.com` does
not become `www.groceries.morrisons.com`. Add a unit test asserting both
cases (bare domain → gets `www.`; already-subdomain domain → left alone).
Done when `npx vitest run` is clean and the new test exists.
*Depends on: nothing. Collision risk: `sitemapCrawl.ts` and its test file
only — no overlap with any other item in this backlog. This is core crawl
code exactly as the brief warns; keep the diff to the URL-building line and
its test, nothing else, so review is fast.*

**4b. Re-run the Asda and Morrisons harvest probes after 4a lands**, one at a
time per the dispatch rule, and confirm the log no longer shows a `www.`
403. This is verification, not a currency confirmation — see item 5, which
is a separate question.
*Depends on: 4a merged. Collision risk: none (probe dispatch only).*

---

## Item 5: Home Bargains / B&M / Morrisons currency confirmation

**Known, fully re-verified against `src/config/retailers.ts`.** All three
(`home-bargains`, `bm-stores`, `morrisons`) are `enabled: false` and present
in the `CURRENCY_UNCONFIRMED` map (confirmed at lines ~5978, ~5962, ~5941).
Home Bargains: 756 URLs, 10/10 priced (comment at line 5593). B&M: 233 URLs,
10/10 priced, sitemap found via robots.txt (comment at line ~5455). Morrisons:
182 URLs, 10/10 priced (line 5187) but its GBP signal is confirmed weak
exactly as the brief describes — read in full at lines 5204–5225:
`parseShopCurrency`'s last-resort bare `"currency":"GBP"` match in homepage
HTML, `/meta.json` 404s, and the specific product URL tried
(`/products/paco-rabanne-pour-homme-aftershave-lotion/113683477`) 404'd
through all six candidate addresses. **`jsonld.ts` confirmed to never read
`priceCurrency`** — it takes the parsed number straight into `priceGbp`
(line 260) — so "10 priced listings" is retrieval-and-parse success, not a
currency finding, for all three shops.

### Sub-tasks

**5a. Home Bargains — one real product-page currency reading.** Take one of
the 10 already-fetched priced URLs from the harvest probe log, run it through
`currency-probe.ts`'s existing methods (the nine-address pattern already used
elsewhere in this file), and read the actual `priceCurrency` off that page,
not the homepage. Done when the registry comment either records a positive
GBP reading with the run/job id, or records that no address returned a
currency at all.
*Depends on: item 4 landed if Home Bargains' domain is `www.`-shaped (check
first — if it is a bare domain this doesn't block). Collision risk: this
edits one entry's comment block in `retailers.ts`; sequence with 5b/5c so two
agents aren't mid-edit on the same file simultaneously — do these three
serially, not in parallel, even though they're independent shops.*

**5b. B&M — same, one real product-page reading**, same method as 5a.
*Depends on: nothing new. Sequence after 5a in the same file.*

**5c. Morrisons — the specific narrower gap already identified:** find a
*current, resolving* product URL on `groceries.morrisons.com` (the one
already tried 404s) and read its `priceCurrency` directly. This is explicitly
smaller than a fresh probe — the brief and the registry comment both say a
single resolving product page is what would clear this entry.
*Depends on: item 4 (Morrisons is a subdomain — `groceries.morrisons.com` —
so the `www.` bug directly affects whether its sitemap crawl succeeds
cleanly; fix 4a first even though the probe log shows the robots.txt
fallback currently papers over it). Sequence after 5a/5b in the same file.*

**5d. On a positive GBP reading for any of the three, flip that entry's
`enabled` and remove it from `CURRENCY_UNCONFIRMED`** in the same commit as
the reading that justifies it — this is the existing house pattern (see the
zimaya/fragrancehub/riiffs/perfumeo precedents already in the file's
comments). Do this per-shop as each clears, not as one batch at the end.

---

## Item 6: Trustpilot ratings

**Known, re-verified.** `trustpilotWidget()` exists at `demo/app.ts:2090`,
called at line 2143. Confirmed **zero** of the 79 retailer entries in
`src/config/retailers.ts` set `trustpilotBusinessId` (`grep -c` returns 0).
The field's doc comment at `src/types/retailer.ts:500-502` confirms two
blocked fetch mechanisms; WebFetch to trustpilot.com is also blocked from
this sandbox (confirmed by the general egress restriction, not separately
tested here). Legitimate route is the Business Units API with a developer
key — **owner action, not approved for this backlog** (explicitly excluded).

### Sub-tasks

**6a (do this one first, independent of the API key). "No reviews available"
fallback.** In `trustpilotWidget()`, when `retailer.trustpilotBusinessId` is
null/unset, render an explicit "No Trustpilot reviews available for
{retailer}" note instead of the current silent no-op. This is the owner's
explicit addition to the brief and ships today regardless of any API key.
Done when every retailer without an id shows the note and `npx vitest run`
covers it (add a test if `demo/app.ts` functions are tested elsewhere in this
style — check `tests/` for an existing `trustpilotWidget` test file first;
if none exists, this is a case for the extraction pattern in hazard D: pull
the widget's pure render logic into its own testable module).
*Depends on: nothing. Collision risk: `demo/app.ts` lines ~2090-2145 — small,
isolated block. Coordinate timing with items 11/15/16 if dispatched the same
day (see hazard D).*

**6b. Leave wiring `trustpilotBusinessId` values and the API-key signup
alone** — both are owner action or explicitly not approved. Do not add a
sub-task that fetches or guesses ids by hand; the doc comment already
explains why two mechanisms were tried and blocked.

---

## Item 7: Fragrantica ratings — explicitly not to be scraped

**Known, partially re-verified.** `docs/SCRAPING.md` (read in full) confirms
Fragrantica is named among sites that blocked this project's own earlier
access attempts (line 22-23) but does **not** itself state the ToS clauses
the brief quotes (automated-access prohibition, commercial-license
requirement for reuse of reviews/notes). Those specific ToS terms are
**reported by the owner brief, not independently re-verified** — confirming
them would mean fetching fragrantica.com's terms page, which is exactly the
kind of access this item forbids attempting, so it was not attempted. Zero
code in the repo touches Fragrantica ratings today (searched, no hits).

### Sub-tasks

**7a. Owner action only — write to Fragrantica for the Commercial License**
their ToS names. Not agent-dispatchable; draft a one-paragraph inquiry if the
owner wants a starting point, but do not send it or treat a reply as
something an agent can obtain.

**No other sub-task.** Do not plan scraping, headless-browser access, or a
"read-only" workaround — the ToS point cited prohibits exactly that, and
`docs/SCRAPING.md` records this project already tried and lost this fight
once. Item 8 is the substitute; it needs no third party.

---

## Item 8: `aggregateRating` from retailer JSON-LD

**Known, fully re-verified.** `grep -rn "aggregateRating|ratingValue|reviewCount"`
across the whole repo returns nothing. `src/catalogue/jsonld.ts` (confirmed)
already parses these same product pages for price data — `aggregateRating`
sits in the same schema.org `Product` object as `offers`, which the existing
parser already reads, so this is additive to code already trusted for
correctness on the same data source.

### Sub-tasks

**8a. Extend `jsonld.ts`'s product parse to also read `aggregateRating`**
(`ratingValue`, `reviewCount`) when present, as an optional field on the
parsed offer — absent when the source page has none, never defaulted or
guessed. Add unit tests with a fixture that has the field and one that
doesn't (schema absence must not throw or silently zero it).
*Depends on: nothing. Collision risk: `jsonld.ts` and its test file only.*

**8b. Thread the new field through the build pipeline** —
`scripts/build-demo-catalogue.ts` picks one rating per fragrance the same way
`pickNotes` picks notes today (and inherits the same "which offer did this
come from" provenance question as items 9/10 — consider doing 8b after 10's
provenance mechanism lands, so both reuse one pattern rather than inventing
two).
*Depends on: 8a. Soft-depends on item 10's provenance work landing first —
not a hard blocker, but doing it after avoids writing the same "which offer"
plumbing twice.*

**8c. Render the rating** wherever it makes sense on the fragrance detail
page in `demo/app.ts` (likely beside where item 11's links land — coordinate
so one agent doesn't touch that block while another is mid-edit).
*Depends on: 8b. Collision risk: `demo/app.ts` fragrance-detail block, same
region as items 11 and possibly 6. Sequence, don't parallelize, against those.*

---

## Item 9: Notes coverage (9,189 products with no notes)

**Known — reported figure, not re-measured today** (the catalogue changes
under an hourly harvest bot, so an exact re-count would already be stale by
the time this document is read; re-measure at dispatch time with
`grep`/a short script over `demo/catalogue.generated.ts` rather than trusting
this number). `pickNotes` (`scripts/build-demo-catalogue.ts:293`, confirmed)
only extracts notes a source explicitly labels ("Top notes:", "Base notes:",
etc.) — the gap is products whose shops simply never publish labelled notes,
not a parsing failure.

**Already in flight** — see hazard A. The hourly backlog routine works this
item directly.

### Sub-tasks

**9a. Re-measure current coverage** at dispatch time (a short script over the
generated catalogue, same shape as item 12's count below) before doing
anything else, since this number moves daily.
*Depends on: nothing. No file collision — read-only.*

**9b. Source notes for products the routine hasn't reached**, batched by
brand or by retailer-source, using only sources this project is entitled to
read (retailer JSON-LD/product-page text — never Fragrantica, per item 7).
This is open-ended research work with no natural end condition beyond
"coverage stopped meaningfully rising" — treat each dispatch as one batch
(e.g., one brand or one retailer's catalogue slice) with its own commit, not
one giant session.
*Depends on: nothing structurally, but **check hazard A first** — don't
duplicate what the hourly routine did in the last cycle.*

---

## Item 10: "As published by X ↗" attribution

**Known, fully re-verified.** `pickNotes(offers)` at
`scripts/build-demo-catalogue.ts:293` takes an `Offer[]` and returns only
`Notes | null` — it does not return which offer the notes came from. That is
the entire blocker: the provenance the owner wants (source name + link) does
not exist anywhere downstream because it is discarded at this one point.
Shares its root cause with item 9 exactly as the brief states — **not** with
item 9's coverage-expansion work itself, only with this one function.

**Already in flight** — see hazard A (routine's item 3, "notes attribution,"
is this exact item).

### Sub-tasks

**10a. Change `pickNotes`'s return shape** to include which offer (retailer
id + product URL) supplied the chosen notes, alongside the notes themselves.
Update its one caller (line 781) and the `Notes`/downstream types
accordingly. Done when `npx tsc` is clean and existing `pickNotes` tests
(check `tests/` for one) still pass with the new shape asserted.
*Depends on: nothing. Collision risk: `scripts/build-demo-catalogue.ts` and
whatever type it exports — **this is the same function item 9's coverage
work reads from**, so if 9b and 10a are dispatched close together, land 10a
first (it's a type/shape change 9b's batches would otherwise need to rebase
past) and tell 9b's agent(s) the new shape exists.*

**10b. Render "As published by {retailer} ↗"** hyperlinked to the specific
product URL from 10a, laid out with the notes as top/middle/base (owner's
explicit ask), with an external-link icon matching the existing icon system
(check `demo/app.ts` for an existing `ICON_EXTERNAL` constant — confirmed
used already at line 2198 in the brand-site link, reuse it rather than adding
a second icon).
*Depends on: 10a. Collision risk: `demo/app.ts`, the notes-rendering block —
coordinate with item 11 if both land the same session, since both add
external, iconed links near the fragrance detail view.*

---

## Item 11: Official Site + Fragrantica links per listing

**Known, fully re-verified.** `officialSiteFor()` exists in
`demo/brandSites.ts` (confirmed, exported at line 930) and is already used
for the brand-page link (`brandView`, line 2168). Re-measured today: **279 of
754** canonical houses resolve via `officialSiteFor` right now (see item 12's
discrepancy note — this moved from the brief's "279 of 723" as the catalogue
grew; the 279-resolved figure itself matches exactly). The brief's "9,216
distinct brand+name pairs" figure is **reported, not re-measured** (a live,
moving count — re-measure at dispatch time the same way item 12 was
re-measured here). Fragrantica *links* (not scraping) are unaffected by
item 7's restriction.

**Already in flight** — hazard A's routine item 4 is this exact item.

### Sub-tasks

**11a. Re-measure the distinct brand+name count** at dispatch time (group
`demo/catalogue.generated.ts` products by brand+name) rather than trusting
the brief's number, since it moves with every harvest.
*Depends on: nothing. Read-only.*

**11b. Build the per-fragrance official-site link.** Reuses `officialSiteFor`
directly — no new lookup needed, just a new call site under the fragrance
title block, left-aligned, same text size as the rest of that block (owner's
spec). Falls back to nothing rendered (not a broken link) when
`officialSiteFor` returns null, same as `brandView` already does.
*Depends on: nothing new — the lookup function already exists and is tested
(`tests/brandSites.test.ts`, confirmed). Collision risk: `demo/app.ts`
fragrance-detail block, same region as 8c and 10b.*

**11c. Build the per-fragrance Fragrantica link.** This needs a URL-shape
function (Fragrantica URLs follow a predictable
`/perfume/{brand-slug}/{name-slug}-{id}.html` pattern, but the numeric id is
not derivable — check whether a Fragrantica URL is already captured anywhere
in existing retailer data, e.g. from an affiliate feed's "as seen on"
references, before assuming one needs constructing). If no reliable URL can
be built without visiting Fragrantica (which item 7 forbids), the honest
sub-task outcome is "link to Fragrantica's own site search for {brand}
{name}" rather than a guessed direct URL — do not construct a URL that might
404 or point at the wrong perfume.
*Depends on: nothing structurally, but resolve the URL-shape question before
writing render code — this is a design decision, not a known fact, and
should not be guessed silently.*

**11d. Render both together** under the title block per the owner's layout
spec, sharing one row/component with 8c and 10b's link styling for
consistency.
*Depends on: 11b, 11c. Collision risk: same `demo/app.ts` region as 8c/10b —
this is the single most contended block in the file; land 8, 10 and 11's
render sub-tasks as one coordinated batch or in a strict sequence, not as
three agents working in parallel.*

---

## Item 12: 444 brands with no website

**Known — re-measured today, and it moved.** I ran `buildBrandCanon` from
`src/catalogue/brandName.ts` over the live `demo/data.ts` catalogue and
`officialSiteFor` from `demo/brandSites.ts` against it just now:

```
total canonical houses: 754   (brief: 723)
resolved:  279   unresolved: 475   (brief: 279 resolved / 444 unresolved)
```

The **resolved count matches exactly** (279); total houses and the
unresolved count are both **31 higher** than the brief states. This is
consistent with catalogue growth since the original measurement — an hourly
harvest bot commits new listings continuously, and 51 of the 754 houses are
the "Armaf - X" fake-brand entries item 14 describes, which inflate this
count until that item is fixed. **Re-measure this number again at dispatch
time**; treat 444/723 as already stale.

**Already in flight** — hazard A's routine item 1 is this exact item, "last
known: 274 of 662."  Note that figure is *also* stale relative to both the
brief's 279/723 and my 279/754 — three different total-house counts across
three points in time, all from a genuinely moving catalogue. This is not a
measurement error anywhere; it's the nature of dispatching against a live,
continuously-harvested dataset — worth telling the owner explicitly so a
"why don't these three numbers match" question doesn't read as a bug.

### Sub-tasks

**12a. Re-measure at dispatch time** (the two-line script above, or ask
whoever dispatches to run it fresh) before batching research.
*Depends on: nothing.*

**12b. Batch brand-website research**, e.g. 30-50 brands per dispatch,
WebSearch only (no arbitrary WebFetch to retailer/brand domains — those are
largely EGRESS_BLOCKED). Each entry added to `demo/brandSites.ts` needs a
real URL found, not guessed, and the UK/non-UK flag set by `marketOf()`
convention already in the file. Done per batch when
`tests/brandSites.test.ts` still passes and the batch's commit names how
many entries were added.
*Depends on: nothing structurally. **Check hazard A first** — the hourly
routine works this file every cycle it picks item 1; a bespoke batch agent
and the hourly routine both editing `demo/brandSites.ts` in the same window
is a real collision risk since it's one flat object literal.*

**12c. Do NOT research "Armaf - X" brands as if they were real houses** —
item 14 will fold ~51 of the unresolved 475 back into "Armaf," which already
has a resolved entry. Researching individual websites for "Armaf - Club De
Nuit" etc. would be wasted work. Sequence item 14 before or alongside 12b,
and exclude anything matching `^Armaf\s*-\s*` from 12b's batches until 14
lands.
*Depends on: awareness of item 14, not a hard code dependency.*

---

## Item 13: 204 non-UK brand links

**Known, fully re-verified — the UK/non-UK split matches the brief exactly**
(uk: 75, non-uk: 204, out of the same 279 resolved entries) even though the
total-house count moved (see item 12). `marketOf()` in
`src/catalogue/brandSiteCheck.ts:118` (confirmed) already computes and labels
this on brand pages.

### Sub-tasks

**13a. For each non-UK-flagged brand, search for a UK-specific storefront**
(`/en-gb/`, `.co.uk`, a UK-market subpath) the way `officialSiteFor`'s
existing entries already do for brands like Acqua di Parma (confirmed
example: `https://www.acquadiparma.com/en/gb/`). Batch this the same way as
12b — 20-30 brands per dispatch, WebSearch only, real URLs only. A brand
genuinely has no UK-specific site is a valid, useful negative result; record
it as checked so a future batch doesn't repeat the search.
*Depends on: nothing structurally besides item 12b having populated the
entries in the first place (13a only applies to already-resolved,
non-UK-flagged entries). Collision risk: same `demo/brandSites.ts` object as
12b — sequence with 12b's batches, don't run both at once against the same
file.*

---

## Item 14: ~50 fake "Armaf - X" brand pages

**Known, re-verified and more precise than the brief.** Counted **51**
houses in the live catalogue matching `^Armaf\s*-\s*` via `buildBrandCanon`
today (brief said "~50" — close enough to confirm, not contradict). This is
a real, existing mechanism in `src/catalogue/brandName.ts`
(`KNOWN_ALIASES`, confirmed at line 70) already used for exactly this shape
of problem — "Paco Rabanne"/"Rabanne", "Armani"/"Giorgio Armani",
"Donna Karan"/"DKNY" are already hand-registered aliases there. This is not
a new mechanism to build, it's ~51 new entries in an existing table, plus
deciding whether the "Armaf - X" product-line words should be preserved
somewhere (e.g. in the fragrance name) rather than simply discarded.

### Sub-tasks

**14a. Confirm the alias-table fix is sufficient** (vs. needing a fix
further upstream, in the feed-ingestion step that first splits brand from
product line) by picking 3-5 of the 51 and checking whether the product-line
words ("Club De Nuit," "Derby," "Ego") already appear in the fragrance's
`name` field independent of the brand string, or only exist in the brand
field. If they only exist in the brand field, folding the brand alone would
silently lose that information from the product name — flag this precisely
before writing the fix.
*Depends on: nothing. Read-only investigation.*

**14b. Add the ~51 aliases to `KNOWN_ALIASES`** in `src/catalogue/brandName.ts`,
each mapping "Armaf - {Line}" → "Armaf", following 14a's finding about
whether the line name needs preserving elsewhere first. Add/extend the
existing brandName test coverage to assert the fold. Done when
`npx vitest run` is clean and a spot-check confirms Armaf's brand page shows
one consolidated house rather than 40+.
*Depends on: 14a. Collision risk: `src/catalogue/brandName.ts` — check
whether hazard-A's hourly routine's item 1 (brand website coverage) has
recently touched this specific file (it usually touches `brandSites.ts`, a
different file, so risk is low but not zero if a routine cycle ever needed
canon changes).*

**14c. Re-run item 12/13's counts after 14b lands** — the fold removes ~51
houses from the unresolved pool (Armaf's own entry is already resolved), so
this is a small, real improvement to 12/13's numbers for free.
*Depends on: 14b.*

---

## Item 15: Brand-direct excluded from Most Stocked / Deals

**Known, fully re-verified — and more specific than the brief.**
`retailersPanel()` already filters `!r.singleBrandOnly` (`demo/app.ts:2058`,
confirmed) so brand-direct shops are correctly hidden from the Retailers
directory already. They are **not** excluded from Most Stocked: I traced the
ranking to `BY_POPULARITY` in `demo/data.ts:156`, sorting on a `popularity`
field set at line 99 as `popularity: entry.shops`, which in turn comes from
`scripts/build-demo-catalogue.ts:779`, `shops: p.offers.length` — a raw count
of every offer, `singleBrandOnly` or not. `scripts/build-demo-catalogue.ts`
confirmed to have **no** `singleBrandOnly`/`enabled` filter in its Deals path
either (`grep` for both terms in `build-deals.ts` returns nothing).

I counted the actual registry: **14** entries currently set
`singleBrandOnly`, of which **11** are also `enabled: true`
(`french-avenue`, `armaf`, `al-haramain`, `riiffs`, `ibraq`,
`bellavita-luxury`, `zimaya`, `kayali`, `zara`, `escentric-molecules`,
`avon`) — not the brief's stated 9. I could not immediately account for the
gap (possibly the brief's "9 contributing" excludes Zara specifically since
item 18 records it currently produces zero listings, and one further shop
whose harvest is similarly empty right now) — worth a quick recount at
dispatch time (`grep -c` pattern used above) rather than trusting either
number blindly.

**An earlier agent was dispatched on this and landed nothing** — worth
finding out why before re-dispatching (check recent branch history for an
abandoned attempt/notes before starting fresh, in case there's a known
blocker not recorded in this brief).

### Sub-tasks

**15a. Re-count `singleBrandOnly` + `enabled: true` entries and cross-check
against which ones actually produce listings right now** (an entry can be
enabled but harvest zero, as item 18 shows for Zara) — this is the number
that actually matters for "contributing," not the config-level count.
*Depends on: nothing. Read-only.*

**15b. Decide where the exclusion belongs — this is a real design fork, not
just a fix.** `entry.shops` (and the `popularity` it feeds) is also the
number shown to shoppers as "stocked by N shops" (confirmed rendered at
`demo/app.ts` lines ~1819, ~2990-2992) — a brand-direct shop selling the
product is true and arguably worth keeping in *that* count even though it
shouldn't drive the *ranking*. The sub-task must decide and record whether
to (a) split `shops` (display) from a new ranking-only count that excludes
`singleBrandOnly`, or (b) exclude `singleBrandOnly` from both. Guess: the
earlier agent that "landed nothing" may have stalled exactly here — surface
this fork explicitly rather than letting a new agent silently pick one.
*Depends on: 15a's real-contribution count, to know how much the choice
actually matters in practice.*

**15c. Implement the Most Stocked exclusion** in
`scripts/build-demo-catalogue.ts`/`demo/data.ts` per 15b's decision, rebuild
(`npm run demo`), and confirm the previously-9-to-11 brand-direct shops no
longer appear in the leading Most Stocked rail while still appearing on
their own fragrance and brand pages.
*Depends on: 15b. Collision risk: `scripts/build-demo-catalogue.ts` (shared
with items 9/10's `pickNotes` work and item 8b — a busy file; sequence
rather than parallelize) and regenerated `demo/catalogue.generated.ts`
(generated — never hand-merge, regenerate on conflict per the repo's own
rule).*

**15d. Implement the Deals exclusion** in `scripts/build-deals.ts` — add the
`!r.singleBrandOnly` filter analogous to `retailersPanel`'s existing one,
rebuild, confirm brand-direct discounts no longer appear in Today's Deals.
*Depends on: nothing beyond 15a's confirmation of which shops actually need
excluding. Collision risk: `scripts/build-deals.ts` and
`demo/deals.generated.ts` (generated) — independent of 15c's files, can run
in parallel with it.*

---

## Item 16: Shipping options on brand pages

**Known, fully re-verified.** The data already exists: `standardGbp`,
`freeDeliveryThreshold` (implied by the field named around line 213 in
`src/types/retailer.ts`) and a delivery-window range are already modelled
per-retailer (confirmed, `src/types/retailer.ts:175-218`) and already feed
per-listing `deliveredPriceGbp` calculations used throughout `demo/app.ts`.
`brandView()` (confirmed, `demo/app.ts:2163-2232`) already finds and
surfaces the brand's own shop (`ownShop`, line 2177) but only renders a
one-line blurb about whether it's compared — none of `standardGbp`,
`freeDeliveryThreshold` or the delivery window actually render there today.

**An earlier agent was dispatched on this and landed nothing too** — same
advice as item 15: check for an abandoned attempt before restarting.

### Sub-tasks

**16a. Render `ownShop`'s delivery terms in `brandView()`** — standard
delivery cost, free-delivery threshold if any, and the delivery window —
using the same formatting helpers already used elsewhere for delivery
figures (check `demo/app.ts` around lines 956-964, 1209-1216 for the existing
"delivery not stated" / unverified-delivery-confidence pattern and reuse it
rather than inventing new copy for the same three states: known-cost,
free-above-threshold, unstated).
*Depends on: nothing new — all the underlying data already exists and is
already read elsewhere in the same file. Collision risk: `demo/app.ts`,
`brandView()` specifically — a different region from the 8c/10b/11d cluster,
lower collision risk with those, but confirm nobody else is mid-edit on
`brandView` (item 15b's decision could theoretically also touch this
function if it changes what's shown alongside `ownShop`).*

**16b. Decide whether this applies to third-party retailers on the brand
page too, or only the brand's own shop** — the owner's ask ("shipping
options on brand pages") is ambiguous between "the brand's own delivery
terms" (what `ownShop` already isolates) and "every listed retailer's
delivery terms, brand page included" (which would duplicate what the
fragrance-detail rows likely already show per-retailer). Resolve this with
the owner or make the narrower reading (own-shop only) the default and note
the broader reading as a follow-up, rather than guessing broad and
over-building.
*Depends on: nothing — this is a scoping question, ideally resolved before
16a's implementation, not after.*

---

## Item 17: Microcopy cull inventory

**Known.** No existing inventory found in the repo (searched `docs/` for
anything matching "microcopy" — no hits). The owner is still deciding on a
previous round; this needs re-producing fresh against the current site
rather than reusing anything old.

### Sub-tasks

**17a. Walk every page type** (home/explore, search, brand, fragrance
detail, retailers directory, changelog, any legal/about pages found in
`demo/legal.ts`) and list every piece of descriptive/explanatory text that
isn't a label, price, or data value — the copy an owner might cut.
*Depends on: nothing. Read-only, no code changed.*

**17b. Produce the inventory as a reviewable list** (page → copy snippet →
where it lives in the source), formatted for the owner to mark Keep/Cut per
line. This is a deliverable in itself, not a precursor to code changes —
**do not act on any Keep/Cut decision in this same task**, since the owner
hasn't made them yet. Consider an Artifact (a simple table) rather than a
markdown file buried in the repo, since this is a decision aid for the owner
to read and mark up, not application documentation.
*Depends on: 17a. No collision risk — this produces no code diff.*

---

## Item 18: Zara + Debenhams (enabled, zero listings)

**Known, re-verified.** Both confirmed `enabled: true` in
`src/config/retailers.ts`. Zara: today's race-timer fix (commit `16297ca`,
confirmed) directly concerns Zara — job 96423941633 answered in 23s but was
killed at 2m04s by the same leaked-timer bug affecting John Lewis/Superdrug,
so "zero listings" may already be stale as of the fix landing today; it needs
re-probing, not re-diagnosing. Debenhams: the brief states its fix is a
deliberately-deferred discovery-pass change affecting URL selection for all
29 shops — **reported, not re-verified** (would require reading the full
discovery-pass logic in `sitemapCrawl.ts`/`strategy.ts`, out of scope for
this planning pass, but flagged as a wide-blast-radius change if picked up).

### Sub-tasks

**18a. Re-probe Zara** via `harvest-one-shop.yml --shop=zara` now that the
timer leak is fixed, one dispatch, confirm from the log whether it now
produces listings. This may simply resolve the item with no code change at
all.
*Depends on: nothing beyond the already-landed timer fix. Collision risk:
none — probe dispatch only, sequence with items 1-3's probes per the cost
rule.*

**18b. If 18a still shows zero listings, diagnose further** (new failure
mode, not the timer leak) before assuming extraction work like items 1-3.
*Depends on: 18a's result.*

**18c. Debenhams: read the deferred discovery-pass fix description in full
before touching it** — this is explicitly the highest-blast-radius item in
the whole 21 (changes URL selection for all 29 shops), and was "deliberately
not done" for a reason not restated in this brief. Do not schedule this as a
routine sub-task; treat it as its own reviewed change with a full
before/after listing-count comparison across all 29 shops, not just
Debenhams.
*Depends on: understanding why it was deferred — find that reasoning (likely
in `docs/EXTRACTION-PLAN.md`, `docs/DECISIONS.md`, or commit history) before
scoping further.*

---

## Item 19: Apify residential proxy failure

**Known, re-verified.** `src/catalogue/apifyProxy.ts` confirmed to build a
standard CONNECT proxy via `undici`'s `ProxyAgent` against
`proxy.apify.com:8000` (lines 30-60). The module's own header comment
(confirmed, lines 12-29) already states this was never run against real
Apify infrastructure before now and that `TypeError: fetch failed` is
undici's generic wrapper with the real reason inside `.cause` — meaning the
actual diagnosis requires reading that `.cause`, not the top-level message,
which the brief's phrasing ("failure is below the credential layer") is
consistent with but doesn't fully resolve.

### Sub-tasks

**19a. Log the full error, including `.cause`, from one real proxied
request** — a one-line change to whatever currently catches and reports the
`TypeError`, run once against real infrastructure via a probe dispatch (not
locally — this sandbox's own egress is restricted, so only a GitHub Actions
runner can meaningfully test this). Confirm from the log what `.cause`
actually says (DNS failure, TLS handshake failure, connection refused, and a
timeout all produce this same top-level message and need different fixes).
*Depends on: nothing. Collision risk: none — isolated diagnostic change to
one file plus one probe dispatch.*

**19b. Fix based on 19a's actual cause**, not the symptom. Scope is unknown
until 19a returns a real answer — do not pre-guess a fix.
*Depends on: 19a.*

---

## Item 20: Changelog entries

**Known, fully re-verified — and see hazard B above, which changes the
recommendation here.** `demo/changelog.ts`'s newest entry today is 18 Aug;
nothing for the 19th or 20th. The four reader-visible changes the brief
lists (Today's Deals to top-level nav, volume filter banded to five ranges,
Most Stocked re-sorted with oils removed, sort control with size ordering)
are plausible reader-visible commits **matching git log subjects seen during
this planning pass** ("Sort control on browse and search, and bottle size as
an ordering," confirmed in `git log`) but I did not independently confirm
all four against full commit bodies — the dispatched changelog Routine
should verify each against real commits itself, per its own existing
instructions, rather than this document asserting all four are correct.

### Sub-tasks

**20a. Do nothing until both changelog Routines have had their scheduled
chance today** (22:08 UTC and 23:55 UTC — see hazard B). This alone may
close the item with zero agent dispatch needed.
*Depends on: time passing. No code.*

**20b. If, after both, `demo/changelog.ts` still has nothing for the 20th**,
dispatch one agent to run exactly what `trig_01PFKVJvLMeWz7UvXdiBCLhY`
already instructs (`npx tsx scripts/changelog-suggest.ts`) and act on its
exit code, rather than hand-rolling a `git log` read — the script exists
specifically so this isn't done ad hoc.
*Depends on: 20a's outcome. Collision risk: `demo/changelog.ts` plus
regenerated `demo/index.html`/`demo/404.html` — low risk once 20a has
confirmed neither Routine got there first, since only one agent would be
writing to it at that point.*

**20c. If both Routines fired and produced two different/conflicting
entries for the same day**, reconcile by hand into one correct entry rather
than leaving both or adding a third — and consider disabling the older,
redundant trigger (`trig_01PFKVJvLMeWz7UvXdiBCLhY`) since the newer one
(`trig_01SsyvoqkDoQo6E3Djjdtsmq`) appears to be its intended replacement.
That's a call for the owner, not an agent, to make.

---

## Item 21: Selfridges Partnerize signup verification

**Known, re-verified as far as this sandbox allows.** All affiliate-network
domains are confirmed `EGRESS_BLOCKED` from this environment (consistent
with every other retailer-domain fetch attempted during this planning pass).
The source article (PerformanceIN, 18 May 2022, Selfridges consolidating
onto Partnerize) is four years old relative to today's date and has not been
re-verified live per the brief — I could not re-verify it either, for the
same egress reason, without a probe dispatch or WebSearch.

### Sub-tasks

**21a. WebSearch (not WebFetch) for anything from 2024-2026 confirming or
contradicting Selfridges-on-Partnerize** — WebSearch is confirmed available
in this environment even though WebFetch to affiliate domains is not.
Look specifically for a *more recent* signal than the 2022 article: a
Partnerize press release, a Selfridges investor/partner page cached in
search results, or a contradicting report of Selfridges moving again. Done
when the finding is written down with what was searched and what came back
— including "found nothing newer" as a valid, useful outcome.
*Depends on: nothing. Collision risk: none — research only, no code.*

**21b. Only after 21a, tell the owner whether the 2022 finding still looks
current** enough to justify the Partnerize signup, or whether it's worth
delaying until a fresher signal turns up. The signup itself
(`partnerize.com/partners`) remains owner action either way — not
agent-dispatchable.
*Depends on: 21a.*

---

## Cross-cutting shared-root clusters

A few items are not independent even though they're numbered separately:

- **9 + 10** share `pickNotes` in `scripts/build-demo-catalogue.ts` as their
  literal root cause. Land 10a (the shape change) before or alongside 9b's
  batches, not after — 9b's batches would otherwise need to be redone against
  the new shape.
- **8b softly depends on 10a** — both need a "which offer did this value come
  from" provenance mechanism; building it once and reusing it avoids two
  slightly different provenance shapes existing side by side.
- **12 + 13 + 14** all read/write the brand-canon surface
  (`brandName.ts`/`brandSites.ts`). Land 14 (the Armaf fold) before or
  alongside 12's research batches so research effort isn't spent on houses
  14 is about to remove.
- **8c + 10b + 11d** are the single most contended region of `demo/app.ts` —
  the fragrance-detail block. Treat these three as one coordinated
  sequence/batch, not three independently-dispatched agents.
- **15c + 9/10's work** both touch `scripts/build-demo-catalogue.ts`;
  sequence rather than parallelize.
- **1c and 2c** both eventually register a new extractor with
  `apifyActor.ts`'s dispatch logic — land one at a time even though the
  extraction work itself is independent per-retailer.

---

## Recommended dispatch order, with reasoning

**Wave 0 — now, zero collision risk, no probe budget spent.** Run these in
parallel; none touch a shared file or a live workflow queue.
1. **4a** (www bug fix) — tiny, isolated, unblocks item 5's Morrisons
   sub-task and fixes a bug already visibly firing in production logs today.
2. **6a** (Trustpilot "no reviews" note) — the owner's explicit, immediate
   ask; ships with no dependency on the API key.
3. **17a/17b** (microcopy inventory) — read-only, produces the artifact the
   owner is waiting on to make Keep/Cut calls.
4. **21a** (Partnerize re-verification via WebSearch) — cheap, prevents the
   owner spending time on a second signup on stale evidence.
5. **14a** (confirm the Armaf alias-fold is sufficient) — read-only
   investigation, unblocks 14b and de-risks 12's research batches.

**Wave 1 — probes, strictly serial, one shop at a time, confirm each job log
before the next.** This is the highest-value unknown-resolving work in the
backlog (three retailers' entire extraction question rides on it) but also
the only real cost center (Apify credit) and the thing that has already cost
three agent groups a batch each — even though I found the specific
concurrency mechanism blamed for that no longer exists (hazard C), the
serial-dispatch discipline stays as cost control regardless.
6. **18a** (re-probe Zara) — cheapest possible win, may close the item with
   zero further work now that the timer bug is fixed.
7. **1a** (John Lewis capture) — most promising of the three per the brief's
   own framing (proven-clean 19-24s retrieval, twice).
8. **2a** (Superdrug capture) — second most promising.
9. **3a** (Selfridges capture) — hardest, do last so the cheaper wins bank
   first and inform how much appetite remains for a possible "no route
   exists" conclusion here.
10. **19a** (Apify proxy real-error capture) — different code path
    (residential proxy, not actor), can interleave with the above without
    adding actor cost, but still one dispatch at a time per the same
    discipline.

**Wave 2 — findings → parsers, after Wave 1's captures return.**
11. **1b → 1c** (John Lewis extractor), **2b → 2c** (Superdrug extractor) —
    sequenced against each other per the shared-registration note above.
12. **3b** (Selfridges go/no-go decision) — may close the item with no
    further code.
13. **18b** (Zara further diagnosis, only if 18a still shows zero).
14. **19b** (proxy fix, based on 19a's real cause).

**Wave 3 — data-model foundation, before the UI work that depends on it.**
Check hazard A immediately before each of these — the hourly routine may
already be advancing them.
15. **14b, 14c** (Armaf alias fold + recount) — small, high-leverage, unblocks
    clean numbers for 12/13.
16. **10a** (pickNotes provenance shape) — foundation for 10b, 8b, and
    9b's future batches.
17. **8a** (jsonld.ts aggregateRating parsing) — independent of 10a, can run
    in parallel with it.
18. **5a, 5b, 5c** (currency readings, serially, same file) — now unblocked
    by 4a for Morrisons specifically.

**Wave 4 — the contended `app.ts` region, as one coordinated batch.**
19. **10b, 8b→8c, 11b, 11c→11d** — land together or in tight sequence, not as
    independently-scheduled parallel agents, per the shared-root note above.
20. **6b** is not a code task (owner action only) — no dispatch.

**Wave 5 — independent app.ts regions and generated-output items, parallel
to Wave 4 since they touch different functions/files.**
21. **15a** (recount) → **15b** (design decision) → **15c** (Most Stocked) +
    **15d** (Deals, parallel to 15c once 15a/15b are settled).
22. **16b** (scope decision) → **16a** (brandView render).

**Wave 6 — open-ended research batches, ongoing rather than one-shot, run
against hazard A's cadence rather than fighting it.**
23. **12b, 13a** (brand-website + UK-market research, batched, sequenced
    against each other since they share `brandSites.ts`).
24. **9b** (notes sourcing batches) — after 10a has landed so batches don't
    need redoing.

**Wave 7 — time-gated, not effort-gated.**
25. **20a** — wait for both changelog Routines' scheduled runs tonight;
    **20b/20c** only if they leave a gap or a conflict.

**Not scheduled at all:** the four Awin applications, John Lewis affiliate
network research, and the Trustpilot API key signup — all explicitly
excluded from this backlog.

**Debenhams (18c)** is deliberately left out of every wave above. It is the
one item in this backlog whose blast radius (all 29 shops' URL selection)
outweighs its individual value, and it was deliberately deferred once
already for reasons this planning pass did not have time to fully recover.
Recommend a dedicated review of *why* before it enters any wave, not a slot
in the queue.

---

## What I found to be wrong or stale when I checked

- **Concurrency hazard (hazard C):** the specific mechanism the brief
  describes — `price-verify.yml` and the probe workflows sharing a
  concurrency group — does not match the current workflow YAML. Three
  separate groups exist (`catalogue`, `price-verify`, and none for the probe
  workflow), and 35 recent probe-workflow runs show no `cancelled`
  conclusions. The probe workflow (`harvest-one-shop.yml`) was created today
  specifically to fix the queuing problem that most likely produced the
  three lost batches referenced. The serial-dispatch discipline is still
  the right call for cost control, just not for the reason originally
  stated.
- **Two changelog Routines, not one** (hazard B) — the brief names only
  `trig_01SsyvoqkDoQo6E3Djjdtsmq`; a second, older, still-enabled one
  (`trig_01PFKVJvLMeWz7UvXdiBCLhY`) also fires today and writes the same
  file.
- **An hourly Routine (hazard A) is already actively working items 9, 10, 11
  and 12** — not mentioned in the brief at all, discovered via
  `list_triggers`. This materially changes the dispatch recommendation for
  four of the 21 items.
- **Brand-count figures moved:** the brief's 723 total houses / 444
  unresolved / 279 resolved is now 754 / 475 / 279 as of this measurement —
  the resolved count and the UK/non-UK split (75/204) matched exactly; the
  totals didn't, consistent with ordinary catalogue growth rather than an
  error in either measurement.
- **Item 15's "9 brand-direct shops enabled"** — I counted 11 entries with
  `singleBrandOnly` set and `enabled: true` in the current registry, not 9.
  Unresolved discrepancy, flagged for a recount at dispatch time rather than
  guessed at.
- Everything else re-checked (items 4, 5, 6, 8, 9's root cause, 10's root
  cause, 12's UK split, 14's alias mechanism, 15's filter locations, 16's
  existing data model, 18's timer-bug link, 19's proxy shape, 20's empty
  changelog) matched the brief.

---

*Written 2026-08-20. Every figure above either names the command/file/line
that produced it or is explicitly marked reported-not-reverified. No
application code was changed to produce this document.*
