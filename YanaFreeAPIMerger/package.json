{
  "name": "yana-free-api-merger",
  "version": "0.1.0",
  "private": true,
  "description": "Perfume chatbot for pricesniffs.space: price checks + notes-based suggestions, answered by an anonymously-ranked council of LLM agents via a self-hosted FreeLLMAPI router.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
  }
}
