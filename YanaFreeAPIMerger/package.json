{
  "name": "yana-free-api-merger",
  "version": "0.2.0",
  "private": true,
  "description": "Virtual Yanny: the fragrance chatbot backend for pricesniffs.space. Answers are grounded only in that site's own live data (see server/siteData.js) and ranked anonymously across an LLM council via a self-hosted FreeLLMAPI router.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "tsx server/index.js",
    "dev": "tsx watch server/index.js",
    "test": "node --import tsx --test",
    "bench": "tsx scripts/bench-deterministic.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "tsx": "^4.19.2"
  }
}
