# Fly.io config for the Virtual Yanny backend.
#
# Deployed only by hand or by .github/workflows/deploy-yanny.yml, always
# from the REPO ROOT (the image needs `demo/` and `src/`, which sit above
# this folder — see Dockerfile's header):
#
#   flyctl deploy . --config YanaFreeAPIMerger/fly.toml \
#                   --dockerfile YanaFreeAPIMerger/Dockerfile --remote-only
#
# ── Changing the app name ────────────────────────────────────────────────
# Fly app names are globally unique across all of Fly, so this one may
# already be taken by someone else. If Fly reports it as unavailable, change
# `app` below to anything free and change nothing else: the deploy workflow
# reads the name from this file, and the public hostname is always
# <app>.fly.dev. (The workflow also takes an `app_name` dispatch input,
# which rewrites this line on the runner for one run, so a collision can be
# worked around without a commit — but the fix belongs here afterwards.)
# This is deliberately a different app from `yanny-freellmapi`, the model
# router — that one is already live and is not touched by any of this.
#
# ── What the rename from "virtual-yanny-backend" was, and was not ────────
# It was renamed on the assumption that the deploy failed because the old
# name was taken. The logs do not support that. Every failed attempt of the
# deploy workflow ended at `flyctl apps create` with:
#
#     Error: unauthorized (Request ID: ...)
#
# which is Fly refusing the *token*, not the name — a deploy token is scoped
# to one app and cannot create apps at all, so it never got as far as
# checking whether "virtual-yanny-backend" was free. Nobody has established
# whether either name is available; that takes a network this repo's tooling
# has never had. The name stays "pricesniffs-yanny" because prefixing with
# the site makes a collision less likely, not because a collision was proved.
app = "pricesniffs-yanny"

# London: the site, its prices and its readers are all UK-facing.
primary_region = "lhr"

# A cushion for the one-time import spike at the first question, so a
# transient peak pages out instead of getting the process OOM-killed
# mid-answer. See the [[vm]] memory note near the bottom for the numbers.
#
# It has to sit up here, above every table header. It used to be the last
# line of this file, after `[[vm]]`, where it was NOT a top-level key at
# all: in TOML every key after a table header belongs to that table, so
# `swap_size_mb` was being parsed as a member of the vm entry —
# `[{memory = "512mb", cpus = 1, cpu_kind = "shared", swap_size_mb = 512}]`
# — which is not where Fly looks for it. Confirmed by parsing this file
# with a TOML library, not by reading it. Best case the swap was silently
# absent; worst case flyctl rejected the config as having an unknown vm
# field, and nothing in this repo had ever run flyctl against it to find
# out.
swap_size_mb = 512

# ── Non-secret configuration ─────────────────────────────────────────────
# These three are public facts and belong in the repo. The fourth env var
# the server reads, FREELLMAPI_API_KEY, is a credential and is NEVER set
# here — it reaches the container only as a Fly secret (`flyctl secrets
# set`), which is stored encrypted by Fly and injected at runtime. Putting
# it in this file would publish it: this repo is public.
[env]
  PORT = "8080"
  FREELLMAPI_BASE_URL = "https://yanny-freellmapi.fly.dev"
  # The ceiling on a single agent call. Note what it is NOT: with the old
  # fan-out — `Promise.allSettled` over all 28 models — it was also the
  # ceiling on the whole question, because nothing could be sent until the
  # last call settled. It no longer is; the two knobs below bound the wait.
  AGENT_TIMEOUT_SECONDS = "25"
  # How the council decides it has waited long enough. See the block comment
  # on COUNCIL_DEADLINE_DEFAULT_MS in server/council.js for what each one
  # trades away — in short, the winner becomes the best of however many
  # answered in time rather than the best of 28, which costs phrasing on the
  # two intents that reach the council at all and cannot cost grounding.
  #
  # Both values are judgements, not measurements: nothing in this repo can
  # reach the router to time a real model. Every question now logs a
  # `[council]` line with its own per-model latencies, so `flyctl logs` is
  # where these should be set from once there is a week of real traffic to
  # read. Raise the quorum if answers read thin; lower the deadline if they
  # read fine and the wait is still long.
  COUNCIL_QUORUM = "8"
  COUNCIL_DEADLINE_MS = "8000"

[http_service]
  # Must match PORT above and ENV PORT in the Dockerfile.
  internal_port = 8080
  # The widget calls this API from an HTTPS page (pricesniffs.space), so a
  # plain-HTTP origin would be blocked by the browser as mixed content.
  # Fly terminates TLS on *.fly.dev automatically; this makes it mandatory.
  force_https = true
  auto_start_machines = true
  # "suspend" rather than "stop": the machine's memory is snapshotted and
  # restored in well under a second, so an idle backend costs nothing but
  # the first reader after a quiet spell does not sit through a cold boot.
  # That matters slightly more now than it used to: the site modules are
  # imported once per process, so a resumed machine keeps the parse it has
  # already paid for, where a full stop/start would pay it again on the
  # next reader's question.
  auto_stop_machines = "suspend"
  # 0 lets the machine idle down when nobody is chatting.
  #
  # ── "it shouldn't go to sleep that fast", costed ────────────────────────
  # Reported by the site's owner and deliberately NOT acted on here: this
  # line is the only lever in this file for it, and moving it spends money,
  # which is not a decision for whoever happens to be editing the config.
  # What is true, and what the options are:
  #
  # This file configures no idle timeout, because there is no key here for
  # one. `auto_stop_machines` and `min_machines_running` are the whole of
  # what is set; how long Fly's proxy leaves an idle machine before
  # suspending it is Fly's own behaviour, not something stated anywhere in
  # this repo. Nobody has been able to check Fly's documentation from the
  # environments this project is edited in (fly.io is blocked at the egress
  # proxy there, as is *.fly.dev), so "is there a longer-idle-period setting"
  # is genuinely open and worth one look at
  # https://fly.io/docs/reference/configuration/ before assuming there is
  # not. If such a key exists it is the cheapest of the options below by a
  # wide margin: a longer nap is free, a machine that never naps is not.
  #
  #   1. Leave this at 0 and warm it earlier. Already done, and free: the
  #      widget now pings /api/health the moment the pointer reaches the
  #      chat launcher, so the resume overlaps with the reader deciding to
  #      click rather than following it (demo/virtualYanny.ts,
  #      warmVirtualYanny). This does not make anything faster — it moves an
  #      unavoidable wait behind something the reader is already doing.
  #   2. min_machines_running = 1. One shared-cpu-1x / 512 MB machine
  #      running 24/7 in lhr, billed continuously instead of per resume.
  #      The rate is on the owner's own Fly plan and is not written here
  #      rather than guessed at; `flyctl` and the Fly dashboard state it
  #      exactly. This removes the resume from the first question of the day
  #      entirely. It does nothing at all for the rest of the latency, which
  #      on a council question is the free-tier router, not this machine.
  #   3. Both. If option 2 is taken, option 1 costs nothing extra and still
  #      helps on the far more common case of a machine that is up but cold
  #      in some other sense.
  #
  # The recommendation is 1 alone for now, and to revisit 2 once the
  # `[council]` log lines (see [env] above) show how much of a real
  # question's wall clock is actually the resume as opposed to the router.
  # Spending money to remove a wait nobody has measured is the wrong order.
  min_machines_running = 0

  # Liveness only, and deliberately so. /api/health always answers HTTP 200
  # — including when it is reporting `ok:false` because the shared upstream
  # router is rate-limited or unreachable (see server/index.js: every
  # failure there is a fact in the body, never a 5xx). So this check tells
  # Fly "the process is up", which is the right thing for Fly to restart
  # machines over. It deliberately does NOT tell Fly "the chatbot works" —
  # that would hand a third party's rate limit the power to take this app
  # down. The semantic check, which reads the JSON body, runs once after
  # each deploy in .github/workflows/deploy-yanny.yml instead.
  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "20s"
    method = "GET"
    path = "/api/health"

[[vm]]
  # `cpus` + `cpu_kind` + `memory` state the machine completely, so the
  # `size = "shared-cpu-1x"` shorthand that used to sit here as well has
  # been dropped: it said the same thing a second way, and a preset
  # alongside the individual keys is one more thing for a future flyctl to
  # disagree with. Nothing about the machine changed with it.
  # ── Why 512 MB and not the 256 MB default ──────────────────────────────
  # Measured, not guessed, and measured again after this app stopped
  # leaking.
  #
  # This used to say 1gb, sized around server/siteData.js re-importing the
  # site's ~15 MB of generated TypeScript on every question under a
  # cache-busted specifier — every one of those a module Node's loader pins
  # for the life of the process. That is fixed: the site modules are now
  # imported once per process (see that file's header for why loading once
  # is the honest answer here and how staleness is reported instead), and
  # what is left is a single flat parse cost rather than a slope.
  #
  # Numbers behind the 512, all taken by driving buildSiteDataBlock() in a
  # loop on this repo's checkout under Node 22 with a ~15 MB catalogue,
  # reading peak RSS from the kernel's own VmHWM and sampling heapUsed
  # after a forced full GC:
  #
  #                        before            after
  #   heapUsed, boot        6.6 MB           6.7 MB
  #   heapUsed, 1 question  209.0 MB         67.7 MB
  #   heapUsed, 50 q        6,530.6 MB       67.8 MB
  #   retained per question +129.0 MB        +0.002 MB
  #   peak RSS, whole run   7,131.6 MB       393.6 MB
  #
  # After the fix, peak RSS landed at 393.6–397.2 MB over five runs of 50
  # to 200 questions and did not move after the first question. 512 MB
  # leaves ~115 MB of headroom over that, and swap below cushions the rest.
  # 256 MB is still not enough: the one-time import of the catalogue peaks
  # well past it.
  #
  # What would make this wrong: the catalogue growing. Peak RSS here is
  # dominated by transpiling and parsing demo/catalogue.generated.ts once,
  # so it tracks that file's size. Re-measure if it passes ~20 MB, and
  # raise this to 1gb if peak RSS gets within ~50 MB of the limit. These
  # figures come from a dev container, not from Fly — nobody has been able
  # to reach Fly from the environment that wrote them.
  memory = "512mb"
  cpus = 1
  cpu_kind = "shared"
